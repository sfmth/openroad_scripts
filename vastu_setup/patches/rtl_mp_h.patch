--- a/tools/OpenROAD/src/mpl/include/mpl/rtl_mp.h
+++ b/tools/OpenROAD/src/mpl/include/mpl/rtl_mp.h
@@ -69,6 +69,18 @@
              const char* report_directory,
              bool keep_clustering_data);
   void blockMacroChannels();
+
+  // Vastu integration: run clustering only and dump to JSON.
+  void dumpClusterTree(int max_num_macro,
+                       int min_num_macro,
+                       int max_num_inst,
+                       int min_num_inst,
+                       float tolerance,
+                       int max_num_level,
+                       float coarsening_ratio,
+                       int large_net_threshold,
+                       const char* output_path);
+
   void placeMacro(odb::dbInst* inst,
                   const float& x_origin,
                   const float& y_origin,
